org.gradle.jvmargs=-Xmx4G
# JDK location comes from the JAVA_HOME environment variable (not hard-coded,
# so this file stays machine-independent and can be committed).
kotlin.incremental=false
flutter.compileSdkVersion=36
# Use AndroidX (and rewrite any legacy support-lib transitive deps to AndroidX)
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
